const ChannelIdLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div className="flex h-full">
      <main className="w-full h-full">{children}</main>
    </div>
  );
};

export default ChannelIdLayout;
